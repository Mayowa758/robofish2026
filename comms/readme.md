# 

## Transmitter

### How controller works
TODO: Replace with USB shield so can use directly with arduinos
In `./transmitter/controller_server.py`, we read in input from the PS4 controller, which then outputs the values to Serial